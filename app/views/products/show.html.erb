<p style="color: green"><%= notice %></p>

<%= render @product %>

<div>
  <%= link_to t("products.edit"), edit_product_path(@product) %> |
  <%= link_to t("products.back"), products_path %>

  <%= button_to t("products.destroy"), @product, method: :delete %>
</div>
